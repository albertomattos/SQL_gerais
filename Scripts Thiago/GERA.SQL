set linesize 1000;
SET HEADING OFF;

SPOOL CORRIGE.SQL;

SELECT
  'ALTER ' || OBJECT_TYPE || ' ' || OBJECT_NAME || ' COMPILE;'
  FROM ALL_OBJECTS
WHERE
  STATUS = 'INVALID' AND
  OWNER = USER;

SPOOL OFF;

 